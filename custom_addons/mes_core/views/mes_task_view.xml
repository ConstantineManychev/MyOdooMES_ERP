<odoo>
    <record id="view_mes_task_tree" model="ir.ui.view">
        <field name="name">mes.task.tree</field>
        <field name="model">mes.task</field>
        <field name="arch" type="xml">
            <tree decoration-danger="priority == 'high'" 
                  decoration-warning="priority == 'medium'" 
                  decoration-success="priority == 'low'"
                  decoration-muted="state == 'done'">
                <field name="priority" widget="badge" 
                       decoration-danger="priority == 'high'" 
                       decoration-warning="priority == 'medium'" 
                       decoration-success="priority == 'low'"/>
                <field name="machine_id"/>
                <field name="name"/>
                <field name="maintainx_current_assignee" string="MX Assignee"/>
                <field name="maintainx_created_at"/>
                <field name="state" widget="badge" 
                       decoration-info="state == 'new'" 
                       decoration-warning="state == 'on_hold'"
                       decoration-primary="state == 'assigned'" 
                       decoration-success="state == 'done'"/>          
            </tree>
        </field>
    </record>

    <record id="view_mes_task_kanban" model="ir.ui.view">
        <field name="name">mes.task.kanban</field>
        <field name="model">mes.task</field>
        <field name="arch" type="xml">
            <kanban default_group_by="state" class="o_kanban_small_column">
                <field name="name"/>
                <field name="user_id"/>
                <field name="machine_id"/>
                <field name="state"/>
                <field name="priority"/>
                <field name="maintainx_id"/>
                <templates>
                    <t t-name="kanban-box">
                        <div class="oe_kanban_global_click oe_kanban_card">
                            <div class="o_kanban_record_top">
                                <div class="o_kanban_record_headings">
                                    <strong class="o_kanban_record_title"><field name="name"/></strong>
                                </div>
                                <div class="o_dropdown_kanban dropdown">
                                    <span t-if="record.priority.raw_value == 'high'" class="fa fa-circle text-danger" title="High"/>
                                    <span t-if="record.priority.raw_value == 'medium'" class="fa fa-circle text-warning" title="Medium"/>
                                    <span t-if="record.priority.raw_value == 'low'" class="fa fa-circle text-success" title="Low"/>
                                </div>
                            </div>
                            <div class="o_kanban_record_body">
                                <span t-if="record.machine_id.raw_value" class="badge text-bg-info">
                                    <field name="machine_id"/>
                                </span>
                                <span t-if="record.maintainx_id.raw_value" class="badge text-bg-dark">MX</span>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <record id="view_mes_task_form" model="ir.ui.view">
        <field name="name">mes.task.form</field>
        <field name="model">mes.task</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <field name="state" widget="statusbar" statusbar_visible="new,assigned,done"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1><field name="name"/></h1>
                    </div>
                    <group>
                        <group string="Main Info">
                            <field name="machine_id"/>
                            <field name="priority" widget="radio" options="{'horizontal': true}"/>
                            <field name="user_id"/>
                        </group>
                        <group string="MaintainX Sync">
                            <field name="maintainx_id"/>
                            <field name="maintainx_created_at"/>
                            <field name="maintainx_current_assignee"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Description">
                            <field name="description"/>
                        </page>
                        <page string="Status History">
                            <field name="status_history_ids">
                                <tree editable="bottom" create="0" delete="0">
                                    <field name="change_date"/>
                                    <field name="status"/>
                                    <field name="source"/>
                                </tree>
                            </field>
                        </page>
                        <page string="Assignees History">
                            <field name="maintainx_assignees_history"/>
                        </page>
                    </notebook>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

    <record id="action_mes_task" model="ir.actions.act_window">
        <field name="name">Tasks</field>
        <field name="res_model">mes.task</field>
        <field name="view_mode">tree,kanban,form</field>
        <field name="context">{'search_default_state': 'new'}</field>
    </record>
    
    </odoo>